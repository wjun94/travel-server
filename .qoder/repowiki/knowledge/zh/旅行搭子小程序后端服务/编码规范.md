- 统一响应格式：所有接口均使用 `pkg/response` 包中的 `Success` 和 `Fail` 方法返回标准 JSON 结构 `{code, msg, data}`。
- 依赖注入方向：Handler 层不直接操作数据库，而是通过调用 `internal/repository` 或 `internal/service` 的方法完成数据持久化与业务处理。
- JWT 鉴权隔离：小程序用户与管理后台使用独立的 JWT Secret 和 Claims 结构，并通过不同的中间件 `JWTAuth` 和 `AdminJWTAuth` 进行拦截。